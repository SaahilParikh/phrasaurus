{
  "name": "phrasaurus-frontend",
  "version": "0.2.0",
  "private": true,
  "description": "Static single-page frontend for phrasaurus. No bundler — serve src/ directly.",
  "type": "module",
  "scripts": {
    "serve": "http-server src -p 8000 -c-1 --cors",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests && prettier --check src tests",
    "format": "prettier --write src tests && eslint --fix src tests"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "http-server": "^14.1.1",
    "prettier": "^3.3.0",
    "vitest": "^2.0.0"
  }
}
